@using CarFuel.Models
@model IEnumerable<Car>
@{
    ViewBag.Title = "Cars";
}

<h2>Cars</h2>


@foreach(var c in Model) {
    <div>
        @c.Make / @c.Modal
    </div>

}
Total = @Model.Count() cars.

